# Changelog

## Unreleased — MathMode V2 evidence runtime

This release line is under review in [PR #1](https://github.com/shaxiaoguang123/math_mode/pull/1).
It is not merged into `main` and is not an official contest submission.

### Added

- Strict workspace-relative JSON contracts and generated schemas.
- Reproducible Python subprocess execution with snapshots, hashes, retry history,
  timeout handling and recovery records.
- Independent numerical validation, evidence gates, assumption sensitivity and
  frozen-result lineage with transitive stale propagation.
- Independently diagnosed and reviewed code repair requests, authorization-bound
  repair execution, explicit repair activation and runtime effective-spec views.
- Explicit owner routing for non-code failure classes, Windows Python 3.11/3.12
  CI, contract fixtures and full skill-mirror parity checks.

### Verification

- Targeted repair/runner and workflow suites pass in Conda `test`.
- GitHub Actions Windows matrix passes on Python 3.11 and 3.12 for commit
  `cb2912e19d8ae981e02eea7b7e17b780045cafbc`.
- The repository retains the original DOCX, TeX class/style/template files,
  images and both complete academic-figure-skill mirrors.

### Limitations

- Official competition policy remains unverified.
- Non-code repair adapters are routing boundaries until owner-specific handoffs
  are implemented.
- Automatic paired rerun, independent revalidation and re-freeze after repair
  activation are not yet orchestrated end to end.
- T09 visual/paper integration and T11 historical finished-problem dry run are
  pending; fixture/provider outputs are not scientific acceptance evidence.
