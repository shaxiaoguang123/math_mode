# 华为杯数学建模项目总控（Claude Code）

本文件是本项目的 Claude Code 级 workflow router。它规定阶段顺序、证据链、自动化门禁和最终完成条件；详细求解规则由 `华为杯_求解规范/华为杯_求解规范.md` 管理，详细绘图规则唯一由 `华为杯_求解规范/华为杯_绘图规范.md` 管理，详细论文/排版规则由 `华为杯_论文规范模板/华为杯_论文章节规范.md`和`华为杯_论文规范模板/README.md` 管理。不要把这些长规范复制进本文件；涉及普通科学结果图时必须发现并调用项目内完整的 `academic-figure-skill`，按需读取其 `SKILL.md`、`references/`、`scripts/` 和 `assets/`。

## 1. 当前审计基线

修改本文件前已递归审计当前项目。当前根目录是一个非 Git 的模板/工具项目；虽已有空的 `题目/`、`数据/`、`求解/`、`论文/` 起始目录以及用于复盘的 `论文.pdf`、`Modex opus5 24华为杯F.pdf`，但尚无可作为下一赛题事实源的真实题目、原始数据或已完成工作目录。两份根目录 PDF 仅用于视觉对比，不得当作下一赛题结果或验收证据。已存在的有效资产包括：

- `华为杯_求解规范/华为杯_求解规范.md`；
- `华为杯_求解规范/华为杯_绘图规范.md`（绘图视觉、图型、Figure Plan、Schematic Plan、Flowchart Plan、质量检查的唯一详细来源）；
- `华为杯_论文规范模板/华为杯_论文章节规范.md`、`README.md`、`论文.tex`、`example.tex`、`gmcmthesis.cls`、`gmcm.bst`、`reference.bib`、`figures/`；
- `第二十三届研赛论文Word标准模板.docx`；
- `优秀论文/A24103350007.pdf`、`优秀论文/C24106130096.pdf`；
- `华为杯_论文规范模板/tools/` 下的 `build_latex.py`、`audit_tex.py`、`migrate_markdown_to_tex.py`、`audit_paper.py`，以及可选 Word 派生工具；
- `.agents/skills/academic-figure-skill/`（Codex 项目级完整绘图 skill）；
- `.claude/skills/academic-figure-skill/`（Claude Code 项目级完整绘图 skill）；
- `华为杯_求解规范/视觉计划.schema.json`、`华为杯_求解规范/tools/init_visual_plan.py`、`audit_visual_plan.py`、`agent_manifest.schema.json`、`章节页数基线.json`、`page_targets.json`和 `验收输出/` 演练材料。
- `华为杯_求解规范/支撑材料清单.schema.json`及 `tools/init_supporting_materials.py`、`build_supporting_materials.py`、`audit_supporting_materials.py`，用于逐问源码、自主数据、补充图表、README 和哈希的支撑材料闭环。

### 1.1 academic-figure-skill 集成边界

出现“绘图/作图/生成结果图/预测图/模型对比图/误差图/敏感性图/鲁棒性图/热图/散点图/箱线图/多面板图”等普通科学结果图请求时，Claude Code 必须发现并调用 `.claude/skills/academic-figure-skill/`，入口为大小写敏感的 `SKILL.md`；运行时按需读取该目录的 `references/`、`scripts/` 和 `assets/figures/`。普通结果图可见文字默认使用中文，并必须通过 `scripts/chinese_fonts.py` 的实际字体/反乱码门禁。Figure Plan 负责提供科学问题、真实结果源、字段、`Figure archetype`、Panel/布局、资产目录、`native run / visual adapt / cross-type inherit`、脚本、PDF/SVG/PNG、统计/QA 报告路径、论文标签和质量状态；同一字段同步写入 `求解/视觉计划.json`，skill 负责普通科研图的图型判断、资产优先、渲染和 QA。

流程图、总体技术路线图、模型结构图和算法流程图不由 skill 替代：它们仍必须走项目 `Flowchart Plan`、真实节点/箭头审查和 TikZ/XeLaTeX 门禁。skill 的 `schematic-led` 或多面板参考只能作为可选参考，不能绕过项目流程图规则。正式普通结果图进入 `求解/问题X/图片/`，并在 `求解/结果索引.md` 登记；不适用的交互式 dashboard、探索性可视化、数学函数图、PowerPoint/Figma 设计和纯代码调试不强制触发该 skill。

`示例输入.json`、示例 DOCX/PDF 和现有 audit 报告是工具链演练（示例 PDF 4 页、正文 1 页的故意负例），不是赛题答案，也不是 45 页通过证据。收到真实赛题后，先在工作目录建立 `题目/`、`数据/`、`求解/`、`论文/`；不要把模板源目录当作工作目录。

## 2. 规则优先级与冲突登记

发生冲突时严格服从：

1. 用户当前明确要求；
2. 当前赛题正文硬约束；
3. 赛题附件说明、官方答案模板、官方公式/程序及提交要求；
4. 当前 `华为杯_求解规范/华为杯_求解规范.md`；
5. 当前详细绘图规范 `华为杯_求解规范/华为杯_绘图规范.md`；
6. 当前 `华为杯_论文规范模板/华为杯_论文章节规范.md`、LaTeX 模板及其 README；
7. `华为杯_论文规范模板/tools/` 的实际接口、`page_targets.json`、schema 和验收证据；
8. 本文件与根目录 `AGENTS.md`；
9. 新导入的旧 `CLAUDE.md`、旧 LaTeX 结构或个人习惯。

已识别的旧规则冲突按以下方式处理：

- 旧 `format.cls`、`fonts/`、默认 `\maketitle`、身份封面、目录和固定总章结构不再是生产规则；当前匿名 LaTeX 生产链优先。
- `example.tex` 仅作宏包/环境参考，不能复制其学校、队号、姓名、默认封面、目录或示例文献。
- 当前论文规范允许按题目自适应章节、普通表格/`tabularx`/`longtable`，不强制所有问题等长或所有表格使用一种环境。
- `论文/论文.tex` 是唯一生产入口，`论文/章节内容/*.tex` 是唯一正文源，使用 25 mm 四边版心；Word 只能由同一 TeX 源派生，不能成为 source of truth。最终 PDF 以 XeLaTeX、`audit_tex.py` 和 `audit_paper.py` 为准。
- 论文章节规范本身不把全文页数写成普遍官方硬限制，但本项目 `page_targets.json` 明确配置正文下限 45 页；因此 **45 页是本项目交付门禁**，不是据此推断其他届次的官方规则。

## 3. 一句话触发的 FULL AUTO / ONE-SHOT MODE

用户输入以下任一等价指令时，进入全流程模式：

- `开始解题`；
- `开始求解`；
- `求解这个题目`；
- `按规则求解`；
- `开始完成论文`；
- `从求解做到论文`；
- `完成这道华为杯赛题`。

全流程模式中，普通方法、模型、图表、章节、文件布局和排版决策由 Claude 根据证据自行完成；不在每问、每张图、论文、Word 生成或页数不足时等待确认。重要方法选择和结论要写入证据/结果索引；不能把 Agent 判断伪装成人工判断。

不得把“全自动”理解为可以猜测、伪造、绕过权限或向外部提交。以下是允许停止的硬阻塞：关键赛题/附件缺失或损坏、证据无法解决的题意/单位冲突、必需软件/许可证/权限不可用、硬约束无法满足、数据矛盾会迫使虚构结果、或连续根因修复后仍无法通过关键门禁。停止时必须报告已完成阶段、精确错误、已尝试修复、现有产物和用户要提供的最小条件；不得只写“运行失败”。

## 4. 不可变输入、工作目录和事实索引

原始题目、原始附件、官方 Word 模板、`gmcmthesis.cls`、`gmcm.bst`、`figures/`和官方样例只读。输出不得覆盖它们。真实项目推荐形成：

```text
.venv/
requirements.txt
题目/                         # 正文、附件说明、官方规则
数据/原始/                    # 原始附件，只读
数据/中间/                    # 解压、清洗、转换副本
数据/自主/                    # 非官方、自主查阅/整理/爬取的数据与来源记录
求解/
  环境与依赖.md
  题面约束清单.md
  数据审计.md
  求解计划.md
  视觉计划.json
  支撑材料清单.json
  结果索引.md
  视觉编码表.md
  AI使用记录.md
  问题一/问题一.py、结果/、图片/
  问题二/...
提交附件/最终提交文件/、支撑材料/、readme.md、_支撑材料构建清单.json、SHA256SUMS.txt
论文/
  论文输入.json
  章节内容/
  验收输出/
```

`求解/结果索引.md`是论文数字、参数、图和表的主索引；重要决策可记录到 `求解/决策记录.jsonl`。事实链必须保持：

```text
原始题目/数据 → 求解代码 → 结构化结果 → 独立验证
→ 结果索引 → 论文内容 → DOCX/PDF
→ 官方附件 + 经审计的源码/自主数据/补充图表支撑材料
```

## 5. Phase 0：环境、项目和工具检查

先读取求解规范，再检查并记录：项目 `.venv`、Python/实际解释器、按需依赖、R/Rscript、TeX Live/XeLaTeX、Microsoft Word COM、`pdfinfo`/PyMuPDF/页面渲染能力和 `华为杯_论文规范模板/tools/` 脚本。求解脚本优先使用项目 `.venv\Scripts\python.exe`；不要无必要安装大型依赖或把模板文档工具的全局环境误当成赛题环境。初始化结果写入 `求解/环境与依赖.md`。

## 6. Phase 1：读题与附件审计

完整读取 `华为杯_求解规范/华为杯_求解规范.md`，递归读取 `题目/`和`数据/`全部相关文件及附件说明，生成 `求解/题面约束清单.md`。逐问记录：输入、输出、目标、依赖、单位、边界、方法限制、CPU/GPU/时间、指标、精度/小数位、训练测试隔离、官方答案文件名/Sheet/列/目录结构/压缩格式、AI 使用要求。不要猜题面没有给出的条件。

随后完成数据审计：格式、大小、Sheet/变量、shape、字段和 dtype、缺失/重复/异常、单位、时间/空间范围、坐标系、标签、泄漏风险和解析失败项。ZIP/RAR 先列目录和规模，只解压工作副本；原始数据只读，清洗写 `数据/中间/`。

赛题官方原始数据之外、实际用于建模的自主查阅/整理/爬取数据统一保存到 `数据/自主/`，同时保留来源、获取日期、许可/条款、采集或整理方法及脚本，不得只在论文中留下 URL。

若用户另行提供往届优秀论文、评阅意见或视觉参考，先生成 `求解/参考论文审阅.md`，逐篇核对问题定义、数据使用、训练/验证切分、指标口径、模型假设、可复现证据和图表结构。参考论文只用于发现候选方法、验证风险和叙事方式；其分数、结论与图片都不是当前赛题事实源。不同切分或不同数据上的指标不得直接排序，不得复制参考论文的数据、结果图或可能存在泄漏的实验设计。

## 7. Phase 2：求解计划

生成 `求解/求解计划.md` 和机器可审计的 `求解/视觉计划.json`。Evidence Matrix 至少审查机理/几何、主结果、独立验证、误差/约束、对比、敏感性/鲁棒性、场景、理论边界与反证；每行填写数据形态、`figure / schematic / table / text / N/A`、结构化结果和关联 ID。Figure Plan 必须在代码前定义科学问题、结果文件、字段/单位、扫描范围/重复次数/统计量、Panel、archetype、hero/layout、输出和质量状态。轨道/空间几何、坐标变换、传播路径、采样/折叠等非流程机理图建立 Schematic Plan 并走 TikZ/XeLaTeX；含步骤、分支、迭代或数据流的结构继续建立 Flowchart Plan。建立任何视觉计划前必须完整读取绘图规范；完成后运行 `audit_visual_plan.py --stage plan`，未通过不得开始求解代码。方法优先级是：题面机理/公式 → 经典数学模型 → 统计/回归/优化 → 传统机器学习 → 深度学习 → 复杂集成。禁止为了论文显得高级而堆模型；题面禁止或不建议机器学习时严格遵守。

计划阶段同时建立“正文证据架构”：把模型动机、推导/算法、主结果、阻断验证、误差结构、敏感性/稳健性和适用边界优先分配到对应问题章节，并预估图表在正文前段、中段和后段的分布。标准/复杂问题逐项审查算法伪代码、机理/流程结构、主结果、比较/消融、独立验证和失效边界；只有实际存在独特算法步骤时才写伪代码，只有结构图能增加可验证信息时才建立 Flowchart Plan。不得预先规划以“完整折级清单”“文件/哈希审计”“序列化与交付完整性”为主要内容的正文独立章；全量逐折表、首行抽查、哈希和构建记录进入附录、结果索引或支撑材料。

问题数量确定后，按 `支撑材料清单.schema.json` 初始化 `求解/支撑材料清单.json`。每问预先登记唯一主入口、完整源码范围、运行记录、外部数据依赖和拟纳入的补充图表；不得等论文结束后凭记忆补清单。

## 8. Phase 3：逐题求解、验证、绘图和留档

按问题一到问题 N形成闭环：

```text
输入 → 数据处理 → baseline → 主模型 → 必要改进
→ 求解 → 独立验证 → 稳定性/敏感性（适用）
→ 结构化结果 → 结果索引 → 科研图 → 官方答案/提交附件
```

每题保留唯一主入口，计算与绘图分离，固定种子并记录时间。对评分公式、可行性、编码、尺寸或答案表要求建立独立验证器，依据原始输入和最终输出重新计算。新增的比较、消融、灵敏度、鲁棒性、场景、Monte Carlo 或稳定性分析必须真实运行并保存到 `求解/问题X/结果/`，绘图只读取这些最终结果。任何绘图前必须再次读取绘图规范；生成、修改或审查任何 Schematic/Flowchart 前还必须再次读取绘图规范和论文章节规范。按 Figure/Schematic/Flowchart Plan 生成图，执行字体、渲染、warning、重叠、裁切、灰度和缩放检查，并把通过状态写入结果索引。图必须支撑结论，不为凑数量生成；简单问题通常至少 2 个 Figure，标准/复杂问题通常 3--6 个，每问必须有主结果图，标准/复杂问题还必须有独立验证图，低于阈值逐项说明不适用理由和替代证据。四问型标准/复杂赛题组合级通常审查 15--25 个非冗余 Figure；一个 Figure 可含多个共同回答同一科学问题的 Panel，数量从不是硬指标。全部普通图生成后运行 `audit_visual_plan.py --stage render`，失败不得进入论文。官方答案文件保留题面要求的文件名、Sheet、结构和精度，保存后重新读取核验。

流程图共享门禁（Codex 与 Claude Code 一致）：流程图必须有真实科学用途；节点、箭头、反馈、分支、验证区和汇总区必须可追溯；类型必须匹配真实模型；Flowchart Plan、TikZ/XeLaTeX、结构真实性、字体、重叠、穿字、颜色、灰度、版心、裁切、缩放、图题、label、正文引用和结果索引均通过后，状态才可标记“通过”并进入论文。

普通结果图先检测实际中文字体，再渲染并检查缺字、负号、标签/图例重叠、裁切、分辨率和论文缩放可读性；失败就修绘图函数并重跑视觉审计。

每问完成时同步更新 `求解/支撑材料清单.json`：列全唯一主入口及其本问/公共依赖源码，保存实际运行命令、通过状态、时间和非空记录；将正文不宜完整展示但可复核的较大篇幅中间图表登记为补充材料；登记本问使用的自主数据及溯源。随后运行 `build_supporting_materials.py` 和 `audit_supporting_materials.py --stage question --question-id <ID>`。该审计未通过时，本问不得标记完成。`提交附件/支撑材料/` 是可重建快照，禁止在其中单独修代码或改数据。

进入论文阶段的门禁：所有问题真实求解；关键结果已保存；独立验证已完成；正式图已生成；官方提交材料（如有）已核验；结果索引可定位论文所需事实；没有已知结果矛盾。缺结果必须回求解阶段，不能在论文中估计或补数。

## 9. Phase 4：论文内容生成

在写论文前完整读取 `华为杯_论文规范模板/华为杯_论文章节规范.md`、`华为杯_论文规范模板/README.md`、`agent_manifest.schema.json`、`章节页数基线.json`和`page_targets.json`。当前主链路是：

```text
真实求解结果 → 论文输入/章节内容/*.tex → 论文/论文.tex
→ 视觉计划 paper 审计 → XeLaTeX 双遍编译 → PDF → 格式与页数验收
```

使用当前 LaTeX 规则生成匿名内容：题目、摘要、关键词位置和字体；正文宋体小四；一级标题黑体四号居中；无身份信息、无页眉、页脚居中页码；摘要/关键词之后自动生成包含三级标题、页码和超链接的目录，再进入正文。正文结构按赛题自适应，至少覆盖需要的问题分析、假设、符号、模型、推导、求解、结果、验证、评价/改进、真实参考文献和必要附录。图表必须在正文引用，且只能引用已按 `华为杯_求解规范/华为杯_绘图规范.md` 通过质量检查的真实结果图；流程图还必须通过 Flowchart Plan、TikZ/XeLaTeX 和结构真实性门禁。论文规范只负责图像路径、宽度、图题、标签、正文引用及 PDF 缩放/裁切检查，不提供固定流程图模板。摘要与正文数字一致，文献真实并逐一引用。最终论文章节禁止使用 Markdown；`.md` 仅用于求解记录、内部审计和 legacy archive。

每个正文一级章必须通过“章节用途门”：它应回答一个独立科学问题、给出模型/证据或形成不可由前章替代的综合结论。若一章主要由逐折明细、特征字典、文件行数、允许值检查、哈希、序列化、人工抽查或提交边界构成，则拆成摘要证据并回填相应问题章节，其余移入附录、`求解/结果索引.md`、验收输出或支撑材料。理论边界、设备诊断和模型选择稳定性原则上与对应问题相邻；不得在论文后半段重新复述前文数字来维持页数。

`agent_manifest.schema.json`要求 `title`、4–6 个 `keywords`、`abstract_tex_path`和章节数组；每章有 `chapter_id`、`title`、`role`、`order`、`tex_path`。禁止 `abstract`、`content`、`content_file` 和 `.md` 章节路径。生成前后都要检查学校/队号/姓名/绝对用户名路径、占位符和虚构结果。

论文内容稳定后再次逐问审查正文未完整展示的长表、批量中间图、分场景/分样本结果，只有真实且具有复核价值的材料才进入支撑包；调试图、缓存、重复图和虚拟环境不得进入。清单、正文、结果索引或官方输出发生变化时必须重建支撑包。

LaTeX 的 `论文/论文.tex` 是唯一提交入口，`gmcmthesis.cls`、`gmcm.bst`和 TikZ 是生产依赖；`example.tex`不作为提交入口，不把旧 `format.cls`、`fonts/`、`\maketitle`、目录或旧固定章节结构带入当前生产链。

## 华为杯赛事标题组件

华为杯论文封面的三行赛事标题必须调用 `gmcm-title.sty` 中的 `\GMCMContestTitle{届次中文数字}`；禁止在新论文中重新手写这三行的字体、字号、伪粗体、阴影和行距参数。当前第二十三届统一调用 `\GMCMContestTitle{二十三}`；届次变化时只修改宏参数（例如 `\GMCMContestTitle{二十四}`），不得修改 `gmcm-title.sty` 的视觉参数。编译引擎必须为 XeLaTeX，并使用系统字体 `STXinwei`；字体缺失时必须报告错误，不得自动字体替代。`gmcm-title.sty` 是华为杯标题格式的唯一事实来源。除非用户明确提出修改官方标题格式，否则 Claude Code 不得调整该组件。

## 10. Phase 5：LaTeX→PDF验收闭环

必须复用当前工具，不另写重复工具：

```text
华为杯_论文规范模板/tools/build_latex.py
→ 华为杯_求解规范/tools/audit_visual_plan.py --stage paper
→ audit_tex.py
→ XeLaTeX × 2
→ audit_paper.py
→ pdfinfo/PyMuPDF/pdftoppm 或 pdftocairo 页面抽检
```

典型根目录命令：

```powershell
python 华为杯_论文规范模板/tools/build_latex.py --manifest 论文/论文输入.json --output 论文/论文.tex --template-dir 华为杯_论文规范模板 --manifest-out 论文/论文.inputs.json
python 华为杯_求解规范/tools/audit_visual_plan.py --plan 求解/视觉计划.json --stage paper --project-root . --main-tex 论文/论文.tex --report 论文/验收输出/视觉计划.paper.audit.json
python 华为杯_论文规范模板/tools/audit_tex.py --manifest 论文/论文输入.json --main 论文/论文.tex --report 论文/验收输出/论文.tex.audit.json
cd 论文
xelatex -interaction=nonstopmode -halt-on-error -file-line-error 论文.tex
xelatex -interaction=nonstopmode -halt-on-error -file-line-error 论文.tex
cd ..
python 华为杯_论文规范模板/tools/audit_paper.py --pdf 论文/论文.pdf --source 论文/论文.tex --targets 华为杯_论文规范模板/page_targets.json --report 论文/验收输出/论文.audit.json
python 华为杯_求解规范/tools/build_supporting_materials.py --manifest 求解/支撑材料清单.json --project-root . --output 提交附件
python 华为杯_求解规范/tools/audit_supporting_materials.py --manifest 求解/支撑材料清单.json --stage final --project-root . --output 提交附件 --report 论文/验收输出/支撑材料.final.audit.json
```

`audit_tex.py`必须通过 TeX-only 输入链、环境、图片、匿名、标签、引用、Unicode 数学字符和正文非斜体检查。编译日志不得有致命错误、字体缺字、未定义引用或不可接受的横向溢出。最终页数只来自 XeLaTeX PDF。Word/DOCX 仅在明确需要时从相同 `.tex` 源派生，并单独运行原有 Word 审计；不得回写正式论文源。

## 11. 正文45页硬门禁与反注水闭环

`page_targets.json`定义：正文从摘要结束后的下一页开始，到参考文献标题前结束；附录不计入正文；当前项目 `required_body_pages` 为 **45**。章节角色窗口（问题章 3–27 页、前置章 1–8 页、评价/结论 1–6 页）是预警，不要求各章等长。

验收流程不是“生成 TeX 即完成”，而是：

```text
生成 TeX → TeX 审计 → XeLaTeX 双遍 → PDF
→ 正文页数 → 各章页数 → 图表/公式/分页/视觉检查
→ 不通过：修改真实内容或布局 → 重新全链路
→ 直到所有硬门禁通过
```

严禁用大面积空白、无意义分页、重复结论/公式/结果/图片、无关背景、虚构实验/数据/结果、装饰性或无意义图、无意义字号/间距/图表放大或压缩官方格式凑 45 页。即使用户允许“好看的无意义图”，也只能把该偏好转化为更高质量的真实证据图，不能削弱事实链。

页数不足时按以下顺序恢复，且每一步都回到对应问题章节，不在论文末尾追加“审计式大章”：

1. 补齐 Evidence Matrix 中真实适用但尚未执行的 Baseline、独立互验、误差/残差、参数可辨识性、输入敏感性、鲁棒扰动、场景、复杂度、收敛、理论边界、约束或反事实；新增实验必须真实运行并保存结构化结果。
2. 写清关键公式的由来、单位、边界、优化目标、停止条件和实际算法；对具有独特循环/分支的算法增加与主入口一致的伪代码，对不同问题确有不同数据流时才增加逐问流程图。
3. 优先把多点扫描、逐折结果、设备/场景差异、误差分布、参数不确定性、特征作用、阈值/边界和模型胜负结构转为非冗余多面板 Figure。每个 Panel 必须有独立问题和真实结果源，不能把同一数字换图型重复展示。
4. 将详细证据前移并均匀嵌入各问；全量逐折表、特征字典、运行日志、文件完整性和哈希移到附录或支撑材料。不得用后置的“完整证据”“交付完整性”章节承载主要页数。
5. 每轮 PDF 都检查正文逐页联系表、章节页数和版面占用。若后 1/3 正文出现连续稀疏页、图表全部集中在前半段、章节主要由短表加大段空白组成，即使总页数达标也必须重新编排；稀疏检测只作结构告警，不能通过放大图表或强制分页消除。

不得用文字膨胀或训练“模型动物园”替代真实证据。页数足够但内容质量或格式不合格仍然失败。

章节标题同页、PDF 中文 CMap 损坏或自动范围结果不合理时，不得把错误的零页/假页数当作通过；使用 Word heading JSON、页脚/页首、渲染页面和 OCR/视觉证据交叉确认。示例验收输出是负例，不能作为真实论文通过证据。

## 12. 修复、回退和最终完成条件

同一根因最多连续尝试三次；然后停止微调，检查数据、接口、单位、约束、假设、环境或重新设计模块。任何下游事实变化都要同步摘要、正文、图表、结果索引和官方附件。永不静默跳过错误、伪造结果或改变题面要求。

只有以下全部满足，才可向用户报告完整流程完成：

1. 全部问题和赛题硬约束已回答；
2. 求解代码真实运行，关键结果有结构化文件和独立验证；
3. `结果索引.md`、AI 记录和官方附件核验完整；
4. 每问完整可运行源码、运行验证、自主数据及溯源、正文未完整展示的补充图表已进入 `提交附件/`，`readme.md` 列出全部文件及作用；
5. `audit_supporting_materials.py --stage final` 通过，支撑材料构建清单、SHA-256、源文件和快照一致且无未登记文件；
6. 论文数字、参数、图、表、摘要和附件一致可追溯；
7. `audit_visual_plan.py --stage paper`和`audit_tex.py`通过；
8. XeLaTeX 双遍成功并导出可读 PDF；
9. `audit_paper.py`无硬失败，正文至少 45 页，章节异常已处理；
10. 视觉、字体、匿名、引用、公式/图表越界检查通过；
11. LaTeX 主线路无致命错误、未定义引用、字体缺失或不可接受溢出；
12. 未向外部系统提交，除非用户另行明确授权。

不得把“生成了示例文件”“TeX 能编译”或“PDF 总页数达到某值”单独报告为完成。
